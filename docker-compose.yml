services:
  web:
    build:
      context: .
    image: paleo-dev:latest
    container_name: paleo-dev
    restart: unless-stopped
    ports:
      - 127.0.0.1:2126:80
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: 3
