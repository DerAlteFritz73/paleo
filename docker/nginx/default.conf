server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html/preview;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~ /\. {
        deny all;
    }
}
