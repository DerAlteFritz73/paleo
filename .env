# Variables attendues par le service "mail" (feedback/send.php), voir
# docker-compose.yml. Les vraies valeurs vivent dans .env.local, qui n'est
# jamais commité — ce fichier ne documente que leur forme.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
FEEDBACK_TO=marino@kreilos.com
